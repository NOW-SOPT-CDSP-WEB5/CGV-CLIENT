export interface MoviePosterType {
	title: string;
	script: string;
	isLiked: boolean;
	isticketed: boolean;
}

export interface MovieHeartType {
	status: number;
	success: boolean;
	message: string;
}